// Stub file generated to satisfy imports when no isolated channels are defined.
// This keeps examples compilable even if the Rust crate doesn't declare any
// events or callbacks. Actual bindings are generated by `flutter_rust_bridge`
// when channels exist, but most examples don't use them.

// nothing to export
